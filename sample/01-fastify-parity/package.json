{
  "name": "nest-native-ai-sdk-fastify-parity",
  "version": "0.0.0",
  "private": true,
  "description": "Sample 01 - @AiStream Express and Fastify adapter parity",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "smoke": "ts-node --transpile-only scripts/smoke.ts",
    "test": "npm run typecheck && npm run smoke",
    "start": "ts-node --transpile-only src/main.ts",
    "start:fastify": "ts-node --transpile-only src/main-fastify.ts"
  },
  "dependencies": {
    "@nest-native/ai-sdk": "0.6.0",
    "@nestjs/common": "12.0.1",
    "@nestjs/core": "12.0.1",
    "@nestjs/platform-express": "12.0.1",
    "@nestjs/platform-fastify": "12.0.1",
    "ai": "^7",
    "class-transformer": "0.5.1",
    "class-validator": "^0.15.1",
    "fastify": "5.12.1",
    "reflect-metadata": "0.2.2",
    "rxjs": "7.8.2",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "ts-node": "10.9.2",
    "typescript": "6.0.3"
  }
}
